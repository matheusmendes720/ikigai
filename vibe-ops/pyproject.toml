[project]
name = "vibe-ops"
version = "0.1.0"
dependencies = [
    "pydantic",
    "sqlalchemy",
    "rich",
    "typer",
    "pyyaml",
    "chromadb",
    "tasklib",
    "python-frontmatter",
    # Local workspace dependency on canonical IKIGAI scoring modules
    "ikigai = {path = \"../life-ops/ikigai\"}",
]
