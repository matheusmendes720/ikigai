{
  "mcpServers": {
    "ikigai": {
      "command": "python",
      "args": ["-m", "ikigai.gateway.start_gateway"],
      "env": {},
      "description": "IKIGAi meta-brain gateway (HTTP+SSE on configurable port)"
    },
    "solverforge-calendar": {
      "command": "python",
      "args": ["-m", "solverforge_calendar.server"],
      "env": {},
      "description": "Calendar planning fork — sf_availability, sf_schedule, sf_replan"
    },
    "tuiboard": {
      "command": "python",
      "args": ["-m", "tuiboard.server"],
      "env": {"TUIBOARD_DATA_DIR": "data"},
      "description": "Kanban rendering fork — tuiboard_diff, tuiboard_snapshot, tuiboard_render, tuiboard_aggregate (wraps all 3 forks)"
    },
    "taskdog": {
      "command": "/usr/bin/bash",
      "args": ["-c", "cd /mnt/c/Users/mathe/code_space/apps/dev-tools/taskdog && uv run taskdog-mcp"],
      "env": {},
      "description": "Task management — 26 tools: list, create, complete, optimize with time tracking"
    }
  },
  "notes": {
    "solverforge": "In-repo Python factory at src/solverforge_calendar/server.py (3 tools)",
    "taskdog_server": "Must be running on :8000 — start with: cd $TASKDOG_ROOT && nohup uv run taskdog-server &",
    "vault_migration": "Vault moved from .omo/ to vault/. MCP server vault reads use data/matheus/ as root.",
    "fork_connection": "solverforge-calendar + tuiboard now connected via Python stdio servers (Phase A1-A4, 2026-08-30). See ADR-012."
  }
}
