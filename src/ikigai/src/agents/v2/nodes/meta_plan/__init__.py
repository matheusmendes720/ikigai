"""meta_plan subgraph nodes (Plan D Task B.1).

TEMPORARY STUB — Plan D D.1 ships before B.1, so this directory currently
contains only a minimal classify_intent used to make D.1's intent-detection
hint work. B.1 will replace the stub with the full classifier + the rest
of the subgraph (fetch / generate / executor).

This file is empty (package marker) — see classify_intent.py.
"""

from __future__ import annotations
